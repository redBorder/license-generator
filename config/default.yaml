swagger:
  fittingsDirs: []
  defaultPipe: null
  swaggerControllerPipe: swagger_controllers

  bagpipes:
    _router:
      name: swagger_router
      mockMode: false
      mockControllersDirs: [ dist/api/mocks ]
      controllersDirs: [ dist/api/controllers ]

    _swagger_validate:
      name: swagger_validator
      validateResponse: true

    swagger_controllers:
      - json_error_handler
      - cors
      - swagger_security
      - swagger_params_parser
      - _swagger_validate
      - express_compatibility
      - _router

    swagger_raw:
      name: swagger_raw
